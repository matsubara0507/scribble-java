package org.scribble.ext.annot.sesstype.name;

// A type that has "annotation" info
// i.e., an annotated payload (data) type, or a PayloadVar occurrence
// Unlike AnnotNode -- InteractionNodes, and no PayloadVarNode
public interface AnnotType
{
	//ScribAnnot getAnnotation();
}
