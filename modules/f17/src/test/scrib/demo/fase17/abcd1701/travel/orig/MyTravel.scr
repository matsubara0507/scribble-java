//$ bin/scribblec.sh modules/f17/src/test/scrib/demo/fase17/abcd1701/travel/orig/MyTravel.scr -f17 Travel

module demo.fase17.travel.abcd1701.orig.MyTravel;

type <java> "java.lang.String" from "rt.jar" as Str;
type <java> "java.lang.Integer" from "rt.jar" as Int;


global protocol Travel(role C, role A, role S)
{
	choice at C
	{
		query(Str) from C to A;
		quote(Int) from A to C;
		do Travel(C, A, S);
	}
	or
	{
		pay(Str) from C to S;
		confirm(Int) from S to C;
		accpt(Int) from C to A;    // Int should same val as the prev
	}
	or
	{
		reject() from C to A;
	}
}
